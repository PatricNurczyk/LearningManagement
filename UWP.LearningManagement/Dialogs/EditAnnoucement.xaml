<ContentDialog
    x:Class="UWP.LearningManagement.Dialogs.EditAnnoucement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Edit Annoucement"
    PrimaryButtonText="Submit"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBox Header="Header:" Text="{Binding Display.Heading, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,25,0,25"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBox Header="Content:" Text="{Binding Display.Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,50"/>
        </Grid>
    </Grid>
</ContentDialog>
